# Unix
alias tlf="tail -f"
alias mkdir='mkdir -p'
alias cl="clear"
alias ll='ls -al'
alias lh='ls -Alh'
# alias -g G='| grep'
# alias -g M='| less'
# alias -g L='| wc -l'
# alias -g ONE="| awk '{ print \$1}'"
